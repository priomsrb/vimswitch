Use `git update-index --skip-worktree README` to ignore this file.

This directory is used as a temporary working area for tests that work on
files.

Everything in this directory gets deleted before and after a test run.
Including this file. But we still need this file in git because git refuses to
add empty folders.